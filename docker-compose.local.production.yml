version: "3"
services:
  sunnycloud:
    build: .
    ports:
      - "8080:8080"
      - "443:443"
    environment:
      - DYNAMODB_ENDPOINT
      - AWS_ACCESS_KEY_ID
      - AWS_SECRET_ACCESS_KEY
      - AWS_REGION
